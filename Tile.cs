class MTile {
    // DONE // public TileType type;
    // DONE // public intVector index;
    // DONE // public MUnit unit;
    // DONE // public MObject mapObject;
    // DONE // public MProvince province;
    // DONE // public bool active;

    // DONE // public bool HasUnit();

    // DONE // public void Hilight(Color color) { TileMap.HilightTile(this, color); }
    // DONE // public void UnHilight() { TileMap.UnHilightTile(this); }

    // DONE // public MTile GetNext(Direction dir) { TileMap.GetNext(this, dir); };

    // DONE // public bool IsActive() => !TileMap.HasActiveMode() || active;
}
